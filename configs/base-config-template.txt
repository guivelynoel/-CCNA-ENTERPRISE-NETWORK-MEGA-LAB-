! ==========================
! BASE DEVICE CONFIG TEMPLATE
! ==========================
conf t
hostname <DEVICE_NAME>
no ip domain-lookup
service password-encryption
enable secret <STRONG_SECRET>

username admin privilege 15 secret <ADMIN_SECRET>

banner motd ^C
UNAUTHORIZED ACCESS IS PROHIBITED.
ALL ACTIVITY IS MONITORED.
^C

line con 0
 logging synchronous
 exec-timeout 10 0
 login local

line vty 0 4
 transport input telnet ssh
 login local
 exec-timeout 10 0

! Optional SSH (requires domain + RSA keys)
ip domain-name lab.local
crypto key generate rsa modulus 1024

end
write memory
